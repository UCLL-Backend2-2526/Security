meta {
  name: Delete actor with ID
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/v1/actors/:actor_id
  body: none
  auth: inherit
}

params:path {
  actor_id: {{actor_id}}
}

settings {
  encodeUrl: true
  timeout: 0
}
